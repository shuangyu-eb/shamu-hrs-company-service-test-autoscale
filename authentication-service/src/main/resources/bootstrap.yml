spring:
  application:
    name: authentication-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        profileSeparator: ','
      discovery:
        instance-id: authentication
        serviceName: authentication-service