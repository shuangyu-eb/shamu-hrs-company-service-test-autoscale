spring:
  application:
    name: company-service
  cloud:
      config:
        enabled: true
        profileSeparator: ','
      discovery:
        instance-id: company
        serviceName: company-service