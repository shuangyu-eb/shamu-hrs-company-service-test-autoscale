spring:
  application:
    name: company-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        profileSeparator: ','
      discovery:
        instance-id: company
        serviceName: company-service